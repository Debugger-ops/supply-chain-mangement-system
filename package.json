{
  "name": "supply-chain-saga",
  "version": "1.0.0",
  "description": "Distributed inventory reservation and saga-based order fulfillment for multi-warehouse supply chains — built to prevent overselling under concurrency and to survive partial failures without manual reconciliation.",
  "main": "dist/api/server.js",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/api/server.ts",
    "build": "tsc -p tsconfig.json && mkdir -p dist/inventory && cp src/inventory/*.lua dist/inventory/",
    "start": "node dist/api/server.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "verify": "tsx scripts/verify.ts",
    "seed": "tsx scripts/seed.ts"
  },
  "keywords": ["supply-chain", "saga", "distributed-systems", "kafka", "redis", "inventory"],
  "license": "MIT",
  "dependencies": {
    "express": "^4.19.2",
    "ioredis": "^5.4.1",
    "kafkajs": "^2.2.4",
    "pg": "^8.12.0",
    "prom-client": "^15.1.3",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.5.0",
    "@types/pg": "^8.11.6",
    "tsx": "^4.19.0",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
